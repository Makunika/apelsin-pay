spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/maxbank
    username: maxbank
    password: maxbank
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQL10Dialect
    hibernate:
      ddl-auto: none

  liquibase:
    change-log: classpath:/db/changelog/db.changelog-master.xml

server:
  port: 2761