spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/maxbank-users
    username: maxbank-users
    password: maxbank-users
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQL10Dialect
    hibernate:
      ddl-auto: none

server:
  port: 5000

encoder:
  sha512:
    salt: saltsalt